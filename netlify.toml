[build]
  publish = "out"
  command = "npm run build"

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "import-artisans.mjs;import-businesses.mjs"
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_URL;NEXT_PUBLIC_SUPABASE_ANON_KEY"